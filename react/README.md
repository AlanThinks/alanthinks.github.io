# React App (Phase 0)

Local development
- cd react
- npm ci
- npm run dev

Build
- npm run build

Notes
- No production changes in Phase 0.
- Routes: /, /projects, /about, /contact
- Styles aggregated from root css into src/styles/legacy.css
- Data helpers import JSON from react/data
